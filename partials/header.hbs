<header class="header site-header exit">
	{{#if @blog.logo}}
	<a class="logo" href="{{@blog.url}}"><img src="{{@blog.logo}}" alt="{{@blog.title}}"></a>
	{{/if}}
	<h2 class="tagline">{{@blog.description}}</h2>
</header>
{{navigation}}